# Spring annotation behavior

This repository contains a small example around Spring's `@Retryable` annotation to explain why it and other Spring annotations sometimes work and other times do not work. 